const EMOTIONS = ['smile', 'sleeping', 'puke', 'angry'];

const FilterType = {
  ALL: 'All movies',
  WATCHLIST: 'Watchlist',
  HISTORY: 'History',
  FAVORITES: 'Favorites',
};

const SortType = {
  DEFAULT: 'default',
  DATE: 'date',
  RATING: 'totalRating',
};

const Mode = {
  DEFAULT: 'DEFAULT',
  DETALS: 'DETALS',
};

const UpdateType = {
  BASE: 'BASE',
  P_DETALS: 'P_DETALS',
};
export {EMOTIONS, FilterType, SortType, UpdateType, Mode};
